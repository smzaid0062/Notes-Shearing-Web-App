import axios from "axios";

export const verifyWithAI = async (subject, text) => {
  // Later connect real AI API
  return {
    match: true,
    confidence: 80,
    reason: "Sample response"
  };
};
